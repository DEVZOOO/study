/**
 * common enum
 */

export enum COMMON {
    APP_NAME = 'Judygram',
    SPLIT_CHAR = '-',
}

// menu enum
export enum NAV_MENU {
    HOME = "home",
    ABOUT = 'about',
    FEED = 'feed',
    SEARCH = 'search',
    ACCOUNT = 'account',
    AUTH = 'auth',
}
